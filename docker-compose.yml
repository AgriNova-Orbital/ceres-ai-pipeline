x-web-healthcheck: &web-healthcheck
  test:
    [
      "CMD",
      "python",
      "-c",
      "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5055/healthz', timeout=3).read()",
    ]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 20s

services:
  redis:
    image: redis:7-alpine
    container_name: ceres-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  web-dev:
    profiles: ["dev"]
    build:
      context: .
      target: dev
    container_name: ceres-web-dev
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - APP_DB_PATH=/app/state/app.db
      - REDIS_URL=redis://redis:6379/0
      - PYTHONUNBUFFERED=1
      - WEBUI_SECRET_KEY=${WEBUI_SECRET_KEY:-ceres-dev-secret}
      - RQ_LOG_LEVEL=DEBUG
      - SENTRY_DSN=${SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-dev}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_TRACES_SAMPLE_RATE=${SENTRY_TRACES_SAMPLE_RATE:-0}
      - SENTRY_PROFILES_SAMPLE_RATE=${SENTRY_PROFILES_SAMPLE_RATE:-0}
      - SENTRY_SEND_DEFAULT_PII=${SENTRY_SEND_DEFAULT_PII:-false}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - SENTRY_LOG_LEVEL=${SENTRY_LOG_LEVEL:-DEBUG}
      - SENTRY_BREADCRUMB_LEVEL=${SENTRY_BREADCRUMB_LEVEL:-DEBUG}
      - SENTRY_EVENT_LEVEL=${SENTRY_EVENT_LEVEL:-ERROR}
      - APP_LOG_LEVEL=${APP_LOG_LEVEL:-DEBUG}
      - CLERK_JWT_ISSUER=${CLERK_JWT_ISSUER:-}
      - CLERK_JWKS_URL=${CLERK_JWKS_URL:-}
      - CLERK_JWT_AUDIENCE=${CLERK_JWT_AUDIENCE:-}
      - CLERK_JWKS_CACHE_TTL_SECONDS=${CLERK_JWKS_CACHE_TTL_SECONDS:-300}
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY:-}
      - NEW_RELIC_APP_NAME=${NEW_RELIC_APP_NAME:-Ceres API Dev}
      - NEW_RELIC_LOG=${NEW_RELIC_LOG:-stdout}
      - NEW_RELIC_DISTRIBUTED_TRACING_ENABLED=${NEW_RELIC_DISTRIBUTED_TRACING_ENABLED:-true}
    ports:
      - "5055:5055"
    volumes:
      - ./state:/app/state
      - ./data:/app/data
      - ./runs:/app/runs
      - ./reports:/app/reports
      - ./logs:/app/logs
    healthcheck: *web-healthcheck
    restart: unless-stopped

  worker-dev:
    profiles: ["dev"]
    build:
      context: .
      target: dev
    container_name: ceres-worker-dev
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    command: ["uv", "run", "python", "-m", "modules.observability", "python", "-m", "modules.jobs.worker", "redis://redis:6379/0"]
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - APP_DB_PATH=/app/state/app.db
      - REDIS_URL=redis://redis:6379/0
      - PYTHONUNBUFFERED=1
      - RQ_LOG_LEVEL=DEBUG
      - SENTRY_DSN=${SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-dev}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_TRACES_SAMPLE_RATE=${SENTRY_TRACES_SAMPLE_RATE:-0}
      - SENTRY_PROFILES_SAMPLE_RATE=${SENTRY_PROFILES_SAMPLE_RATE:-0}
      - SENTRY_SEND_DEFAULT_PII=${SENTRY_SEND_DEFAULT_PII:-false}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - SENTRY_LOG_LEVEL=${SENTRY_LOG_LEVEL:-DEBUG}
      - SENTRY_BREADCRUMB_LEVEL=${SENTRY_BREADCRUMB_LEVEL:-DEBUG}
      - SENTRY_EVENT_LEVEL=${SENTRY_EVENT_LEVEL:-ERROR}
      - APP_LOG_LEVEL=${APP_LOG_LEVEL:-DEBUG}
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY:-}
      - NEW_RELIC_APP_NAME=${NEW_RELIC_WORKER_APP_NAME:-Ceres Worker Dev}
      - NEW_RELIC_LOG=${NEW_RELIC_LOG:-stdout}
      - NEW_RELIC_DISTRIBUTED_TRACING_ENABLED=${NEW_RELIC_DISTRIBUTED_TRACING_ENABLED:-true}
    volumes:
      - ./state:/app/state
      - ./data:/app/data
      - ./runs:/app/runs
      - ./reports:/app/reports
      - ./logs:/app/logs
    restart: unless-stopped

  frontend-dev:
    profiles: ["dev"]
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}
        SENTRY_ENVIRONMENT: ${SENTRY_ENVIRONMENT:-dev}
        SENTRY_RELEASE: ${SENTRY_RELEASE:-local}
        NEXT_PUBLIC_SENTRY_ENABLE_LOGS: ${NEXT_PUBLIC_SENTRY_ENABLE_LOGS:-true}
        NEXT_PUBLIC_SENTRY_ENVIRONMENT: ${SENTRY_ENVIRONMENT:-dev}
        NEXT_PUBLIC_SENTRY_RELEASE: ${SENTRY_RELEASE:-local}
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
    container_name: ceres-frontend-dev
    depends_on:
      web-dev:
        condition: service_healthy
    environment:
      - API_URL=http://web-dev:5055
      - SENTRY_DSN=${SENTRY_DSN:-}
      - NEXT_PUBLIC_SENTRY_DSN=${NEXT_PUBLIC_SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-dev}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - NEXT_PUBLIC_SENTRY_ENABLE_LOGS=${NEXT_PUBLIC_SENTRY_ENABLE_LOGS:-true}
      - NEXT_PUBLIC_SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-dev}
      - NEXT_PUBLIC_SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY:-}
    ports:
      - "3002:3000"
    restart: unless-stopped

  web-beta:
    profiles: ["beta"]
    build:
      context: .
      target: beta
    container_name: ceres-web-beta
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - APP_DB_PATH=/app/state/app.db
      - REDIS_URL=redis://redis:6379/0
      - PYTHONUNBUFFERED=1
      - WEBUI_SECRET_KEY=${WEBUI_SECRET_KEY:-ceres-beta-secret}
      - RQ_LOG_LEVEL=INFO
      - SENTRY_DSN=${SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-beta}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_TRACES_SAMPLE_RATE=${SENTRY_TRACES_SAMPLE_RATE:-0}
      - SENTRY_PROFILES_SAMPLE_RATE=${SENTRY_PROFILES_SAMPLE_RATE:-0}
      - SENTRY_SEND_DEFAULT_PII=${SENTRY_SEND_DEFAULT_PII:-false}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - SENTRY_LOG_LEVEL=${SENTRY_LOG_LEVEL:-INFO}
      - SENTRY_BREADCRUMB_LEVEL=${SENTRY_BREADCRUMB_LEVEL:-INFO}
      - SENTRY_EVENT_LEVEL=${SENTRY_EVENT_LEVEL:-ERROR}
      - APP_LOG_LEVEL=${APP_LOG_LEVEL:-INFO}
      - CLERK_JWT_ISSUER=${CLERK_JWT_ISSUER:-}
      - CLERK_JWKS_URL=${CLERK_JWKS_URL:-}
      - CLERK_JWT_AUDIENCE=${CLERK_JWT_AUDIENCE:-}
      - CLERK_JWKS_CACHE_TTL_SECONDS=${CLERK_JWKS_CACHE_TTL_SECONDS:-300}
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY:-}
      - NEW_RELIC_APP_NAME=${NEW_RELIC_APP_NAME:-Ceres API Beta}
      - NEW_RELIC_LOG=${NEW_RELIC_LOG:-stdout}
      - NEW_RELIC_DISTRIBUTED_TRACING_ENABLED=${NEW_RELIC_DISTRIBUTED_TRACING_ENABLED:-true}
    ports:
      - "5155:5055"
    volumes:
      - ./state:/app/state
      - ./data:/app/data
      - ./runs:/app/runs
      - ./reports:/app/reports
      - ./logs:/app/logs
    healthcheck: *web-healthcheck
    restart: unless-stopped

  worker-beta:
    profiles: ["beta"]
    build:
      context: .
      target: beta
    container_name: ceres-worker-beta
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    command: ["uv", "run", "python", "-m", "modules.observability", "python", "-m", "modules.jobs.worker", "redis://redis:6379/0"]
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - APP_DB_PATH=/app/state/app.db
      - REDIS_URL=redis://redis:6379/0
      - PYTHONUNBUFFERED=1
      - RQ_LOG_LEVEL=INFO
      - SENTRY_DSN=${SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-beta}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_TRACES_SAMPLE_RATE=${SENTRY_TRACES_SAMPLE_RATE:-0}
      - SENTRY_PROFILES_SAMPLE_RATE=${SENTRY_PROFILES_SAMPLE_RATE:-0}
      - SENTRY_SEND_DEFAULT_PII=${SENTRY_SEND_DEFAULT_PII:-false}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - SENTRY_LOG_LEVEL=${SENTRY_LOG_LEVEL:-INFO}
      - SENTRY_BREADCRUMB_LEVEL=${SENTRY_BREADCRUMB_LEVEL:-INFO}
      - SENTRY_EVENT_LEVEL=${SENTRY_EVENT_LEVEL:-ERROR}
      - APP_LOG_LEVEL=${APP_LOG_LEVEL:-INFO}
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY:-}
      - NEW_RELIC_APP_NAME=${NEW_RELIC_WORKER_APP_NAME:-Ceres Worker Beta}
      - NEW_RELIC_LOG=${NEW_RELIC_LOG:-stdout}
      - NEW_RELIC_DISTRIBUTED_TRACING_ENABLED=${NEW_RELIC_DISTRIBUTED_TRACING_ENABLED:-true}
    volumes:
      - ./state:/app/state
      - ./data:/app/data
      - ./runs:/app/runs
      - ./reports:/app/reports
      - ./logs:/app/logs
    restart: unless-stopped

  frontend-beta:
    profiles: ["beta"]
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}
        SENTRY_ENVIRONMENT: ${SENTRY_ENVIRONMENT:-beta}
        SENTRY_RELEASE: ${SENTRY_RELEASE:-local}
        NEXT_PUBLIC_SENTRY_ENABLE_LOGS: ${NEXT_PUBLIC_SENTRY_ENABLE_LOGS:-true}
        NEXT_PUBLIC_SENTRY_ENVIRONMENT: ${SENTRY_ENVIRONMENT:-beta}
        NEXT_PUBLIC_SENTRY_RELEASE: ${SENTRY_RELEASE:-local}
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
    container_name: ceres-frontend-beta
    depends_on:
      web-beta:
        condition: service_healthy
    environment:
      - API_URL=http://web-beta:5055
      - SENTRY_DSN=${SENTRY_DSN:-}
      - NEXT_PUBLIC_SENTRY_DSN=${NEXT_PUBLIC_SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-beta}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - NEXT_PUBLIC_SENTRY_ENABLE_LOGS=${NEXT_PUBLIC_SENTRY_ENABLE_LOGS:-true}
      - NEXT_PUBLIC_SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-beta}
      - NEXT_PUBLIC_SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY:-}
    ports:
      - "3102:3000"
    restart: unless-stopped

  web-release:
    profiles: ["release"]
    build:
      context: .
      target: release
    container_name: ceres-web-release
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - APP_DB_PATH=/app/state/app.db
      - REDIS_URL=redis://redis:6379/0
      - PYTHONUNBUFFERED=1
      - WEBUI_SECRET_KEY=${WEBUI_SECRET_KEY:-ceres-release-secret}
      - RQ_LOG_LEVEL=WARNING
      - SENTRY_DSN=${SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-release}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_TRACES_SAMPLE_RATE=${SENTRY_TRACES_SAMPLE_RATE:-0}
      - SENTRY_PROFILES_SAMPLE_RATE=${SENTRY_PROFILES_SAMPLE_RATE:-0}
      - SENTRY_SEND_DEFAULT_PII=${SENTRY_SEND_DEFAULT_PII:-false}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - SENTRY_LOG_LEVEL=${SENTRY_LOG_LEVEL:-INFO}
      - SENTRY_BREADCRUMB_LEVEL=${SENTRY_BREADCRUMB_LEVEL:-INFO}
      - SENTRY_EVENT_LEVEL=${SENTRY_EVENT_LEVEL:-ERROR}
      - APP_LOG_LEVEL=${APP_LOG_LEVEL:-INFO}
      - CLERK_JWT_ISSUER=${CLERK_JWT_ISSUER:-}
      - CLERK_JWKS_URL=${CLERK_JWKS_URL:-}
      - CLERK_JWT_AUDIENCE=${CLERK_JWT_AUDIENCE:-}
      - CLERK_JWKS_CACHE_TTL_SECONDS=${CLERK_JWKS_CACHE_TTL_SECONDS:-300}
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY:-}
      - NEW_RELIC_APP_NAME=${NEW_RELIC_APP_NAME:-Ceres API Release}
      - NEW_RELIC_LOG=${NEW_RELIC_LOG:-stdout}
      - NEW_RELIC_DISTRIBUTED_TRACING_ENABLED=${NEW_RELIC_DISTRIBUTED_TRACING_ENABLED:-true}
    ports:
      - "5255:5055"
    volumes:
      - ./state:/app/state
      - ./data:/app/data
      - ./runs:/app/runs
      - ./reports:/app/reports
      - ./logs:/app/logs
    healthcheck: *web-healthcheck
    restart: unless-stopped

  worker-release:
    profiles: ["release"]
    build:
      context: .
      target: release
    container_name: ceres-worker-release
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    command: ["uv", "run", "python", "-m", "modules.observability", "python", "-m", "modules.jobs.worker", "redis://redis:6379/0"]
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - APP_DB_PATH=/app/state/app.db
      - REDIS_URL=redis://redis:6379/0
      - PYTHONUNBUFFERED=1
      - RQ_LOG_LEVEL=WARNING
      - SENTRY_DSN=${SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-release}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_TRACES_SAMPLE_RATE=${SENTRY_TRACES_SAMPLE_RATE:-0}
      - SENTRY_PROFILES_SAMPLE_RATE=${SENTRY_PROFILES_SAMPLE_RATE:-0}
      - SENTRY_SEND_DEFAULT_PII=${SENTRY_SEND_DEFAULT_PII:-false}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - SENTRY_LOG_LEVEL=${SENTRY_LOG_LEVEL:-INFO}
      - SENTRY_BREADCRUMB_LEVEL=${SENTRY_BREADCRUMB_LEVEL:-INFO}
      - SENTRY_EVENT_LEVEL=${SENTRY_EVENT_LEVEL:-ERROR}
      - APP_LOG_LEVEL=${APP_LOG_LEVEL:-INFO}
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY:-}
      - NEW_RELIC_APP_NAME=${NEW_RELIC_WORKER_APP_NAME:-Ceres Worker Release}
      - NEW_RELIC_LOG=${NEW_RELIC_LOG:-stdout}
      - NEW_RELIC_DISTRIBUTED_TRACING_ENABLED=${NEW_RELIC_DISTRIBUTED_TRACING_ENABLED:-true}
    volumes:
      - ./state:/app/state
      - ./data:/app/data
      - ./runs:/app/runs
      - ./reports:/app/reports
      - ./logs:/app/logs
    restart: unless-stopped

  frontend-release:
    profiles: ["release"]
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}
        SENTRY_ENVIRONMENT: ${SENTRY_ENVIRONMENT:-release}
        SENTRY_RELEASE: ${SENTRY_RELEASE:-local}
        NEXT_PUBLIC_SENTRY_ENABLE_LOGS: ${NEXT_PUBLIC_SENTRY_ENABLE_LOGS:-true}
        NEXT_PUBLIC_SENTRY_ENVIRONMENT: ${SENTRY_ENVIRONMENT:-release}
        NEXT_PUBLIC_SENTRY_RELEASE: ${SENTRY_RELEASE:-local}
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
    container_name: ceres-frontend-release
    depends_on:
      web-release:
        condition: service_healthy
    environment:
      - API_URL=http://web-release:5055
      - SENTRY_DSN=${SENTRY_DSN:-}
      - NEXT_PUBLIC_SENTRY_DSN=${NEXT_PUBLIC_SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-release}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - SENTRY_ENABLE_LOGS=${SENTRY_ENABLE_LOGS:-true}
      - NEXT_PUBLIC_SENTRY_ENABLE_LOGS=${NEXT_PUBLIC_SENTRY_ENABLE_LOGS:-true}
      - NEXT_PUBLIC_SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-release}
      - NEXT_PUBLIC_SENTRY_RELEASE=${SENTRY_RELEASE:-local}
      - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY:-}
    ports:
      - "3202:3000"
    restart: unless-stopped
